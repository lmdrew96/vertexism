import ProseSection from "@/components/ProseSection";

const SectionIV = () => {
  return (
    <ProseSection id="section-iv">
      <h2 className="section-head">
        <span className="numeral">IV.</span>Connection is the divine.
      </h2>

      <p>
        Yes, a vertex is technically the <em>point</em> where two lines meet.
        That&rsquo;s the whole idea — a vertex without edges is just a dot
        floating in empty space. What makes a vertex a vertex is its
        connections. Every other theology worships the dot. Vertexism says the
        dot is only a portion of what matters — only a mere product of
        connection. Every dot is inherently neutral and full of potential; our
        own free will determines our fate.
      </p>

      <p>
        You won&rsquo;t find the divine in any one place or thing, but in the
        spaces between — in the relationships themselves. Sanctity lives in
        the connections between all experiences.
      </p>

      <p className="lead">Edges exist in one of four states.</p>
    </ProseSection>
  );
};

export default SectionIV;
